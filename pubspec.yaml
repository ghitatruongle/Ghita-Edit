name: ghita_edit
description: Cross-platform multimedia editor prototype with native C++ engine via Dart FFI.
publish_to: 'none'

# Version is derived from lib/src/core/version.dart — do not edit directly!
# Generated by: flutter pub run build_runner build --release
version: 0.4.0+4

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  ffi: ^2.1.2
  file_picker: ^8.1.4
  path: ^1.9.0
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png

