# ---- Build artifacts (in-source Ninja build) ----
CMakeCache.txt
CMakeFiles/
.ninja_deps
.ninja_log
.qt/
GhitaEdit.exe
GhitaEdit_autogen/

# ---- Deploy / staging dirs ----
build-win/
build-linux/
build-test/
build-binmodel/
release/
vcpkg_installed/
vcpkg_buildtrees/

# ---- Logs ----
*.log
aqtinstall.log
build_reconf.log
vcpkg-manifest-install.log

# ---- Tooling / editor ----
.zcode/

# ---- Visual Companion scratch (brainstorm mockups) ----
.superpowers/

# ---- Qt deploy DLLs dropped next to the in-source exe by windeployqt ----
Qt6*.dll
Qt6*.pdb
vc_redist*.exe

# ---- FFmpeg / D3D deploy DLLs in source root (windeployqt) ----
av*.dll
sw*.dll
d3dcompiler_47.dll

# ---- in-source Ninja build files ----
build.ninja
cmake_install.cmake

# ---- Qt plugin deploy dirs in source root (windeployqt on in-source exe) ----
generic/
iconengines/
imageformats/
networkinformation/
platforms/
qmltooling/
tls/

# ---- misc deploy DLLs (FFmpeg/x264/PortAudio/DirectX/ANGLE) ----
dxcompiler.dll
dxil.dll
libx264-164.dll
portaudio.dll
opengl32sw.dll

# ---- generated by CMake ----
ghita_qml.qrc

# ---- local temp files ----
pid.txt
rebuild.bat

# ---- windeployqt pollutes source qml/ with Qt's QML plugins ----
# (runtime imports are resolved from the deployment dir, not here)
qml/Qt/
qml/QtQuick/
qml/QtQml/
